export interface SocialIconProps {
  src: string;
  alt: string;
}

export interface FooterLinkProps {
  title: string;
  links: string[];
}

export interface PartnerProps {
  type: string;
  logo: string;
  name: string;
  alt: string;
}
